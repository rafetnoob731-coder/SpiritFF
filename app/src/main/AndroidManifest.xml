<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="false"
        android:label="SpiritFF"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.SpiritFF">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Our overlay service — no foregroundServiceType needed -->
        <service
            android:name=".service.FloatingOverlayService"
            android:exported="false"
            tools:remove="android:foregroundServiceType"/>

        <!-- Shizuku user service -->
        <service
            android:name=".service.ShizukuUserService"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL"
            tools:remove="android:foregroundServiceType"/>

        <!-- Strip specialUse from any Shizuku internal services that get merged -->
        <service
            android:name="rikka.shizuku.ShizukuService"
            tools:node="remove"/>
        <service
            android:name="rikka.shizuku.server.ShizukuService"
            tools:node="remove"/>

    </application>
</manifest>
